export const createNewUser = (firstName, lastName, email, password, username, userChats, userFriends) => ({
    firstName,
    lastName,
    username,
    email,
    password,
    userChats,
    userFriends,
});
